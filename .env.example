# Polkadot Configuration
# =====================
# Your seed phrase (12 or 24 words) for the account that will pay transaction fees
# KEEP THIS SECURE! Never commit this file with real values to version control
NOMINATOR_SEED=your_seed_phrase_here

# The validator address for which to claim staking rewards
VALIDATOR_ADDRESS=1VALIDATOR_ADDRESS_HERE

# SMTP Email Configuration
# =========================
# SMTP server hostname (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=smtp.example.com

# SMTP port (typically 587 for STARTTLS, 465 for SSL/TLS)
SMTP_PORT=587

# Use secure connection (true for port 465, false for port 587)
SMTP_SECURE=false

# Email address for SMTP authentication
SMTP_USER=your_email@example.com

# SMTP password or app-specific password
# For Gmail, you need to create an app-specific password:
# https://support.google.com/accounts/answer/185833
SMTP_PASS=your_smtp_password_here

# Email address to receive notifications
SMTP_TO=notification_recipient@example.com
